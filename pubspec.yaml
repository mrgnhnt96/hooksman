name: git_hooks
version: 0.0.1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  args: ^2.6.0
  change_case: ^2.1.0
  equatable: ^2.0.7
  file: ^7.0.1
  glob: ^2.1.2
  mason_logger: ^0.3.1

dev_dependencies:
  autoequal_gen: ^0.9.1
  build_runner: ^2.4.13
