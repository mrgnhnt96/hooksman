# Managed by hooksman

Do not edit files in this directory. Shims are regenerated by
`dart run hooksman register`.

After cloning, run register so `.dart_tool/hooksman/executables/` exists;
these shims invoke those binaries.

Set `HOOKSMAN=0` (or `SKIP=1`) to no-op all hooks.
